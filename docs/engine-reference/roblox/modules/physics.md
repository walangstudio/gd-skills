# Roblox 2025+ — Physics Reference

> Pinned to Roblox 2025+. Verify anything newer against https://create.roblox.com/docs.

## Core types & entry points
- `BasePart` + physics props (`Anchored`, `AssemblyLinearVelocity`)
- Constraints: `AlignPosition`, `LinearVelocity`, `Rope`, `WeldConstraint`
- `Touched` event; `workspace:Raycast(origin, dir, params)`
- Collision groups via `PhysicsService`

## See also
- `roblox-patterns` skill, `roblox-style` rule, `roblox-specialist` agent
- guides/ for cross-engine architecture patterns

> TODO: expand verified snippets and gotchas.
